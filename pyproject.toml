[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "osimfit"
version = "0.1.0"

[project.optional-dependencies]
test = ["pytest"]

[tool.hatch.build.targets.wheel]
packages = ["src/osimfit"]

[tool.pytest.ini_options]
testpaths = ["tests"]
